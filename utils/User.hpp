#ifndef User_hpp
#define User_hpp
#include "Common.hpp"
class User {
    private:
        string userName;
        string userId;
        int userAge;

        // Transaction transactionList*
    public:
        //create a constructor for objects of the class

        //write a fuction that let the user borrow a book

        //write a fucntion that let the user return the book

        //write a fucntion that let the users see their own transaction history

        //write a function that display the user information
};
#endif